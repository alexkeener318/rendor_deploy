/**
* This context serves as a global variable which contains the current language selected by the user
* @author   David Asatryan
*/
import { createContext } from 'react'

export const LanguageContext = createContext("en");