/**
* This context serves as a global variable which contains the current total of the user's order
* @author   David Asatryan
*/
import { createContext } from 'react'

export const UserContext = createContext(14);